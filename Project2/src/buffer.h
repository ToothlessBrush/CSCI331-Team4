#include <string>
#include <map>
#include <vector>

struct ZipCodeData
{
    int zip_code;
    std::string place_name;
    std::string state;
    std::string county;
    float latitude;
    float longitude;
};

class Buffer
{
private:
public:
};